/** Flag global que usan los formularios para avisar al layout de tabs
 *  que hay cambios sin guardar. El layout muestra un diálogo ModernAlert
 *  directamente — este módulo ya no contiene lógica de UI. */
export const formGuard = {
  hasUnsaved: false,
};
